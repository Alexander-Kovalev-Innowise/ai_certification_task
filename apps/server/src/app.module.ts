import { Module } from '@nestjs/common';

// Placeholder — guard pipeline (JwtAuthGuard, RolesGuard, CapabilitiesGuard,
// ThrottlerGuard) is registered in Task 2.8. Feature modules are added per phase.
@Module({
  imports: [],
})
export class AppModule {}
